
<div ng-controller="BriefsCtrl">
  <div id="brief-list-container">
    <ul>
      <li ng-repeat="brief in briefs" ng-class="isSelected(brief)" ng-click="showBrief(brief)" class='menu-brief'>
        <h3>{{brief.name}} <small>({{brief.due_date}})</small></h3>
      </li>

    </ul>
     <a href="#create">create</a>
  </div>
  


  <div id="brief-view-container" ng-show="selectedBrief" >
  <h2>{{selectedBrief.name}}</h2>
    <form editable-form name="editableForm" onaftersave="saveBrief()">
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Project Name: </span>
        <span editable-text="selectedBrief.name" e-name="name" onbeforesave="checkName($data)" e-required>{{ selectedBrief.name || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Project Instructions: </span>
        <span editable-textarea="selectedBrief.instructions" e-name="instructions">{{ selectedBrief.instructions || 'empty' }}</span>
      
      </div>

      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Issue Date: </span>
        <span editable-text="selectedBrief.issue_date" e-name="issue_date" >{{ selectedBrief.issue_date || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Due Date: </span>
        <span editable-text="selectedBrief.due_date" e-name="due_date" >{{ selectedBrief.due_date || 'empty' }}</span>
      
      </div>
       <div>
      <!-- editable username (text with validation) -->
        <span class="title">Contact Name: </span>
        <span editable-text="selectedBrief.contact_name" e-name="contact_name" >{{ selectedBrief.contact_name || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Contact: </span>
        <span editable-text="selectedBrief.contact" e-name="contact" >{{ selectedBrief.contact || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Website: </span>
        <span editable-text="selectedBrief.website" e-name="website" >{{ selectedBrief.website || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Budget: </span>
        <span editable-text="selectedBrief.budget" e-name="budget" >{{ selectedBrief.budget || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Type of Work: </span>
        <span >{{ selectedBrief.type_of_work || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Audience: </span>
        <span editable-text="selectedBrief.audience" e-name="audience" >{{ selectedBrief.audience || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Simliar Work: </span>
        <span editable-text="selectedBrief.similar_work" e-name="similar_work" >{{ selectedBrief.similar_work || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Client Information: </span>
        <span editable-text="selectedBrief.client_information" e-name="client_information" >{{ selectedBrief.client_information || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Purpose: </span>
        <span editable-textarea="selectedBrief.purpose" e-name="purpose" >{{ selectedBrief.purpose || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Competitors: </span>
        <span editable-text="selectedBrief.competitors" e-name="competitors" >{{ selectedBrief.competitors || 'empty' }}</span>
      
      </div>
      <div>
      <!-- editable username (text with validation) -->
        <span class="title">Internal Resources: </span>
        <span editable-text="selectedBrief.internal_res" e-name="internal_res" >{{ selectedBrief.internal_res || 'empty' }}</span>
      
      </div>
    
        <div class="buttons">
      <!-- button to show form -->
          <button type="button" class="btn btn-default" ng-click="editableForm.$show()" ng-show="!editableForm.$visible">Edit</button>
          <button type="button" class="btn btn-default" ng-click="addProjectElements(selectedBrief.id)" ng-show="!editableForm.$visible">See Project Elements</button>

      <!-- buttons to submit / cancel form -->
          <span ng-show="editableForm.$visible">
            <button type="submit" class="btn btn-primary" ng-disabled="editableForm.$waiting">Save</button>
            <button type="button" class="btn btn-default" ng-disabled="editableForm.$waiting" ng-click="editableForm.$cancel()">Cancel</button>
          </span>
        </div> 
      </form>
     
  </div>
 
</div>